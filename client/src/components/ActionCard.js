import React from 'react';
import { Card, Typography } from '@material-ui/core';

const ActionCard = () => {
  return (
      <Card>
          <Typography>
          Action Card
          </Typography>
      </Card>
  );
};

export default ActionCard;