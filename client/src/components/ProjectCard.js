import React from 'react';
import { Card, Typography } from '@material-ui/core';

const ProjectCard = () => {
  return (
      <Card>
          <Typography>
          Action Card
          </Typography>
      </Card>
  );
};

export default ProjectCard;